// Jest setup file for common test configuration

// Increase timeout for database operations
jest.setTimeout(30000);

// Mock console methods if needed during testing
// global.console = {
//   ...console,
//   // Uncomment to silence logs during tests
//   // log: jest.fn(),
//   // info: jest.fn(),
//   // warn: jest.fn(),
//   // error: jest.fn(),
// };
